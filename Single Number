#include<stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int ar[n] , cnt[n] , res = 0;
    for(int i = 0 ; i < n ; i++){
        cnt[i] = 0;
        scanf("%d",&ar[i]);
        res ^= ar[i];
    }
    printf("%d",res);
    
}
